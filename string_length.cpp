#include<iostream>
#include<string.h>
using namespace std;
int main ()
{
    char str[50];
    int len;
    cout << "Enter an array or string : ";
    gets(str);
    length = strlen(str);
    cout << "Length of the string is : " << length;
    return 0;
}